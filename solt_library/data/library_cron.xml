<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!-- Cron to check due dates daily -->
    <record id="ir_cron_library_due_dates" model="ir.cron">
        <field name="name">Library: Check Due Dates</field>
        <field name="model_id" ref="model_library_loan"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_due_dates()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active">True</field>
        <field name="doall" eval="False"/>
    </record>

    <!-- Cron to check expired memberships -->
    <record id="ir_cron_library_memberships" model="ir.cron">
        <field name="name">Library: Check Expired Memberships</field>
        <field name="model_id" ref="model_library_member"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_expired_memberships()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active">True</field>
    </record>
</odoo>