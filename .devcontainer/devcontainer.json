// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-existing-docker-compose
{
	"name": "Soltein Odoo Training",

	// Update the 'dockerComposeFile' list if you have more compose files or use different names.
	// The .devcontainer/docker-compose.yml file contains any overrides you need/want to make.
	"dockerComposeFile": [
		"docker-compose.yml"
	],

	// The 'service' property is the name of the service for the container that VS Code should
	// use. Update this value and .devcontainer/docker-compose.yml to the real service name.
	"service": "solt-odootraining",

	// The optional 'workspaceFolder' property is the path VS Code should open by default when
	// connected. This is typically a file mount in .devcontainer/docker-compose.yml
	"workspaceFolder": "/workspaces/solt-odootraining",

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [8069, 8071, 8072, 5432],

	// Uncomment the next line if you want start specific services in your Docker Compose config.
	// "runServices": [],

	// Uncomment the next line if you want to keep your containers running after VS Code shuts down.
	// "shutdownAction": "none",

	// we are going to install some python deps
	// into the user's home directory
	// so we need to add it to the path
	"containerEnv": {
		"PATH": "${PATH}:/home/vscode/.local/bin"
	},
	"remoteEnv": {
		"PATH": "${containerEnv:PATH}"
	},

	// Uncomment the next line to run commands after the container is created.
	"postCreateCommand": "pip install -r .devcontainer/requirements_dev.txt",

	// Configure tool-specific properties.
	"customizations": {
		"vscode": {
			"extensions": [
				// csv
				"mechatroner.rainbow-csv",

				// docker
				"ms-azuretools.vscode-docker", 
				// python
				"ms-python.python", "ms-python.vscode-pylance", "ms-python.isort", 

				// python linters (type checker)
				"matangover.mypy",

				// python linters (errors)
				"ms-python.flake8",

				// python linters (security)
				"ms-security.bandit",

				// python linters (formatting)
				"ms-python.black-formatter",

				// python linters (docstring)


				// python linters (complexity)
				"ChristianDein.python-radon",

				// odoo
				"trinhanhngoc.vscode-odoo",

				// postgresql
				"mtxr.sqltools", "mtxr.sqltools-driver-pg"
			]
		}
	},

	// Uncomment to connect as an existing user other than the container default. More info: https://aka.ms/dev-containers-non-root.
	"remoteUser": "vscode"
	

}
